apiVersion: v1
kind: Service
metadata:
  name: {{ template "muximux.fullname" . }}
  labels:
    app: {{ template "muximux.name" . }}
    chart: {{ template "muximux.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: {{ template "muximux.name" . }}
    release: {{ .Release.Name }}
